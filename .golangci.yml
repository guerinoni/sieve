version: "2"
linters:
  default: all
  disable:
    - wsl
    - varnamelen
    - paralleltest
    - ireturn
    - depguard
    - lll

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - funlen
          - cyclop
